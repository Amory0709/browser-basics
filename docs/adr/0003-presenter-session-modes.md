# Presenter controls follow vs free via session modes

Participants do not choose whether to follow the presenter. The presenter switches the room between **follow mode** (viewport sync enforced via existing `globalFollow` / follow map) and **free mode** (participants interact independently). This is a first-class room concern stored in shared state, not a per-participant preference.
